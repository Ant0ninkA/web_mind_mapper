# react-scripts@5 lists an advisory `peerOptional typescript@^4`, but we run
# TypeScript 5 (CRA transpiles via Babel at runtime, so this is safe). Without
# this, every `npm install` fails with ERESOLVE. Keep until react-scripts is
# replaced (e.g. by Vite).
legacy-peer-deps=true
